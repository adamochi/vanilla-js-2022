// #3.0 the document object
// document
// document.title will show "learning"
// you can update as well
// document.title = "Hi"
document.title = "Hello, from JS!";
// the document is literally your website

/*
function mergeArrays(arr1, arr2) {
  let arr3 = new Uint8Array(arr1.concat(arr2)).sort();
  return arr3;
}

console.log(mergeArrays([5, 6, 7, 8], [1, 2, 3, 4]));

function spinWords(string) {
  //splits string into words separated by a space
  let splitStringArray = string.split(" ");

  for (let i = 0; i < splitStringArray.length; i++) {
    //if the word is more than 5 chars, reverse the word
    if (splitStringArray[i].length >= 5) {
      splitStringArray[i].split("").reverse().join("");
    }
  } //end for loop

  //join the modified array
  const joinString = splitStringArray.join(" ");

  return joinString;
} //end function

console.log(spinWords("Hey fellow students"));

// function spinWords(string) {
//   const words = string.split(" ");
//   if (words.length >= 7) {
//     words.reverse();
//     return words.join("");
//   }
// }
console.log(spinWords("helpmeplease hsaa kds k"));
console.log(spinWords("help"));
console.log(spinWords("helpmeplease"));
*/
